const clamp=(n,min=0,max=1)=>Math.min(max,Math.max(min,n));const heroMedia=document.querySelector('.hero-media');const targets=[...document.querySelectorAll('.reveal-section,.project,.news-card,footer')];const scrubHeads=[...document.querySelectorAll('[data-scrub]')];scrubHeads.forEach(el=>{const text=el.textContent;el.innerHTML='';[...text].forEach(c=>{const s=document.createElement('span');s.className='char';s.textContent=c;el.appendChild(s)})});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;if(entry.target.classList.contains('project')){const i=[...document.querySelectorAll('.project')].indexOf(entry.target);entry.target.style.animationDelay=`${i*80}ms`}entry.target.classList.add('in')}),{threshold:.13,rootMargin:'0px 0px -4%'});targets.forEach(el=>observer.observe(el));let ticking=false;function update(){const y=scrollY,vh=innerHeight;heroMedia.style.transform=`translate3d(0,${y*.32}px,0) scale(${1+y*.000035})`;scrubHeads.forEach(el=>{const r=el.getBoundingClientRect(),p=clamp((vh*.88-r.top)/(vh*.74+r.height)),chars=[...el.children],active=Math.floor(p*(chars.length+8));chars.forEach((c,i)=>c.classList.toggle('on',i<active))});ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});addEventListener('resize',update);update();const cursor=document.querySelector('.cursor');if(matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>cursor.style.transform=`translate3d(${e.clientX-12}px,${e.clientY-12}px,0)`);document.querySelectorAll('a').forEach(a=>{a.addEventListener('mouseenter',()=>cursor.classList.add('big'));a.addEventListener('mouseleave',()=>cursor.classList.remove('big'))})}
