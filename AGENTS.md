# 项目协作记录

## 项目约定
- 本仓库是统一网页展示站；每个新网页放入独立子目录，不再新建 GitHub 仓库。
- 根目录维护项目索引页；Loops 页面位于 `/loops/`。
- 视觉目标：高保真复刻 Loops Framer 首页。
- 所有交互元素移除浏览器默认焦点轮廓。
- 保留响应式布局、滚动入场与按钮交互。

## 2026-08-13
- 初始化 Vite 静态前端项目。
- 完成导航、首屏、产品展示、能力卡片、流程、口碑、CTA 与页脚。
- 下载并本地化原站核心图片资源。
- 修复直接通过 `file://` 打开时 ES Module 被浏览器拦截导致的空白页，并将图片改为本地相对路径。
- 经 `http://localhost:5173/` 真实浏览器验证，页面结构、样式与核心图片均正常显示。
- 补充 Framer 风格动效层：首屏编排、宣言逐词显现、卡片交错入场、主视觉视差和减少动态效果适配。
- 恢复功能四宫格内部演示动画：集成开关轮换、搜索自动输入、分类标签轮播、指派对象切换。
- 将项目调整为可持续扩展的 Web Showcase：新增根目录项目索引、`loops/` 子路径与 GitHub Pages 部署流程。
