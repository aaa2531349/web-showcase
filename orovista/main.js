const reveals=[...document.querySelectorAll('.reveal')],observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');observer.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -5%'});reveals.forEach((el,i)=>{el.style.transitionDelay=`${(i%3)*70}ms`;observer.observe(el)});
const counters=[...document.querySelectorAll('[data-count]')],counterObserver=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=+el.dataset.count,suffix=el.textContent.includes('%')?'%':'+';const start=performance.now();const tick=now=>{const p=Math.min(1,(now-start)/1200);el.textContent=Math.round(target*(1-Math.pow(1-p,3)))+suffix;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);counterObserver.unobserve(el)}),{threshold:.6});counters.forEach(el=>counterObserver.observe(el));
const parallax=[...document.querySelectorAll('.parallax-frame img')];let ticking=false;function update(){const h=innerHeight;parallax.forEach(img=>{const r=img.parentElement.getBoundingClientRect(),p=(h-r.top)/(h+r.height);img.style.setProperty('--parallax',`${(p-.5)*-130}px`)});const hero=document.querySelector('.hero h1');hero.style.transform=`translateY(${scrollY*.22}px)`;hero.style.opacity=Math.max(0,1-scrollY/600);ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});update();
const menu=document.querySelector('.menu'),open=document.querySelector('.menu-btn'),close=document.querySelector('.menu-close');function setMenu(show){menu.classList.toggle('open',show);menu.setAttribute('aria-hidden',String(!show));document.body.style.overflow=show?'hidden':''}open.addEventListener('click',()=>setMenu(true));close.addEventListener('click',()=>setMenu(false));menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.querySelector('form button').addEventListener('click',e=>{e.currentTarget.textContent='MESSAGE READY';setTimeout(()=>e.currentTarget.textContent='SEND MESSAGE',1800)});
