# Gent 子项目协作记录

## 项目约定
- 参考页面：`https://tough-truly-243033.framer.app/`。
- 通过 `/gent/` 访问，保留原始响应式布局、分词入场、滚动视差、出场和微动画。
- 所有交互元素移除浏览器默认焦点轮廓。
- 本页面属于根目录 Web Showcase，不新建独立仓库。

## 2026-08-13
- 完成参考站全页结构、桌面分段滚动、触发动画与交互取证。
- 新增独立 Gent 展示入口，以无边框全视口方式承载参考 Framer 页面，确保复杂动画逐帧一致。
- 保留参考站的完整视口几何关系，避免改变 Framer 基于视口计算的入场、固定导航与视差轨迹。
- 按需求将右下角 “Made in Framer” 固定品牌标识移出父页面可视裁切区域。
- 将整页视觉由绿色主题统一调整为黑白主题，保留原有光影层级、素材、视差和动画行为。
- 根据视觉反馈将黑白主题调整为暗蓝主题，绿色泛光映射为深海蓝高光，保持白色文字和原动画结构。
- 视觉对比后撤回暗蓝方案，恢复黑白主题作为当前版本。
- 压低黑白主题的全局高亮上限，将功能卡和其他卡片中的纯白底统一收敛为低亮银灰，减少刺眼感。
- 按反馈将黑白主题的统一高亮强度进一步调整为 20%。
- 纠正亮度调整范围：撤销整页 20% 亮度处理，后续仅针对功能卡素材中的亮色背景调整。
- 将参考页运行壳本地化，黑白滤镜移入页面内部；仅将四张功能卡黑色 UI 外围的亮色背景层透明度设为 20%，不影响整页和卡片内容。
- 将四张功能卡黑色 UI 外围的亮色背景层透明度由 20% 进一步调整为 10%。
- 接入根目录项目索引与统一 Vite 构建流程。
